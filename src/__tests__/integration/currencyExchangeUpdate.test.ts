// todo: add test case here
// data should be fetched immediately from the privat api
// validated, parsed, saved into db and fetched from the db

describe("exchange rate fetch from privat api and save into db", () => {
  it.skip("should add new exchange rate to db properly", () => {});
});
